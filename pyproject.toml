[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "goose-playground"
version = "0.1.0"
description = "Playground for testing Goose"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
